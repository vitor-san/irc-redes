#include <iostream>

using namespace std;

int main() {
    return

        using namespace std;
    0;
}80socklensocklen_t            close())serserver;
;
.sin_family = AF_INET, .sin_port = htons(80),
                       .sin_addr.s_addr = htonl(INADDR_ANY)
}
;
.si;
;
sockaddr_caddrcaddrcaddr cuout >> << ""
                                     "PAi ai tá online\n;\n